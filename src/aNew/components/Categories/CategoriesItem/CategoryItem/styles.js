import { StyleSheet } from 'react-native';

const container = {
  paddingVertical: 10,
  paddingLeft: 10,
  borderBottomWidth: StyleSheet.hairlineWidth,
};

const categoryIcon = {
  flexDirection: 'row',
  alignItems: 'center',
};

const iconItem = { marginRight: 15 };
styles = StyleSheet.create({ container, categoryIcon, iconItem });

export default styles;
