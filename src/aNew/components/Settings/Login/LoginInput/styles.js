import { StyleSheet } from 'react-native';

const container = { marginBottom: 15 };
const textInput = {
  borderBottomWidth: 1,
  borderColor: '#000',
  padding: 0,
};

styles = StyleSheet.create({ container, textInput });

export default styles;
