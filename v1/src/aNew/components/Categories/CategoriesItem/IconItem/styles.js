import { StyleSheet } from 'react-native';

const iconStyle = {
    borderColor: '#ff5a19',
    borderWidth: 3,
    borderRadius: 12,
    width: 45,
    height: 45,
    overflow: 'hidden',
    justifyContent: 'center',
    alignItems: 'center',
  },
  styles = StyleSheet.create({ iconStyle });

export default styles;
