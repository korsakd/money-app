import { StyleSheet, TextStyle } from 'react-native';

const container = { marginBottom: 20 };
const textInput: TextStyle = {
  borderBottomWidth: 1,
  fontSize: 17,
  padding: 0,
};

export default StyleSheet.create({ container, textInput });
